<div class="p-6 my-5 bg-white rounded-sm">
  <p class="text-lg"><p>
  <div class="px-6 my-5">
    <div class="pb-5">
      <p class="font-semibold">Name</p>
      <p><%= @guest.first_name %> <%= @guest.last_name %></p>
    </div>
  </div>
</div>

<div class="pt-5">
  <%= link_to "Edit guest", edit_guest_path(@guest), class: "button" %>
</div>
